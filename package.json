{
  "name": "jarvis-ai-assistant",
  "version": "1.0.0",
  "description": "Google Workspace AI Assistant with Jarvis personality",
  "scripts": {
    "start": "cd jarvis-backend && gunicorn --bind 0.0.0.0:$PORT --workers 2 wsgi:application",
    "dev": "concurrently \"cd jarvis-backend && python src/main.py\" \"cd jarvis-frontend && npm run dev\"",
    "install-all": "cd jarvis-backend && pip install -r requirements.txt && cd ../jarvis-frontend && npm install"
  },
  "engines": {
    "node": ">=20.18.0",
    "python": ">=3.11.0"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ai", "assistant", "google-workspace", "jarvis", "flask", "react"],
  "author": "Manus AI",
  "license": "MIT"
}

